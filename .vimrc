# show existing tab with 4 spaces width
set tabstop=4
set number

# To be able to see coloured text in vi editor
# Although this will give an error when sourcing .vimrc, it still works. Don't know why.
syntax on
